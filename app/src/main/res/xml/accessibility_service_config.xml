<?xml version="1.0" encoding="utf-8"?>
<!--
    Hosts the TYPE_ACCESSIBILITY_OVERLAY window without SYSTEM_ALERT_WINDOW.
    The service also tracks the foreground package and, when the assistant tile is enabled,
    reads accessibility window content to extract assistant response text locally.
    No accessibility content is transmitted off-device by this app.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeNotificationStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
